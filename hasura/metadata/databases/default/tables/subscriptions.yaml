table:
  name: subscriptions
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - organization_id
        - granularity
        - upload_limit
        - download_limit
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
    comment: ""
