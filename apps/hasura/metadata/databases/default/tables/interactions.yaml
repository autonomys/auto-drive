table:
  name: interactions
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - account_id
        - type
        - size
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
    comment: ""
